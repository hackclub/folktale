[package]
name = "folktale"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-files = "0.6.10"
actix-web = "4.14.1"
pulldown-cmark = { version = "0.13.4", default-features = false, features = ["html"] }
